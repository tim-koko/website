import ScrollOut from 'scroll-out';

ScrollOut({
    targets: '.banner',
    //offset: -100,
    threshold: 0.9,
  });
  
  ScrollOut({
    /* options */
    //threshold: 0.5,
    //offset: -100,
    once: true,
  });